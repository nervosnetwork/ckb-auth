[workspace]
members = ["contracts/auth-rust-demo"]
exclude = ["tests"]

[workspace.package]
edition = "2024"

[profile.release]
overflow-checks = true
strip = true
opt-level = 's'
lto = false
codegen-units = 1
